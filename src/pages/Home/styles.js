import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  firstBlock: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      minHeight: 500,
  },
  firstRow: {
      maxWidth: 700,
  }
}));
