import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    container: {
        padding: "20px",
        backgroundColor: "white",
        borderRadius: "10px",
        marginTop: "20px",

    },
    items: {
        background: "red",
        padding: "10px",
    },

 
}));
